import styled from 'styled-components';
import theme from '../../../utils/theme';

const Apropos = styled.div`
  border: 1px solid ${theme.colors.cook};
  border-radius: 4px;
  padding: 10px;
`;

export default Apropos;
