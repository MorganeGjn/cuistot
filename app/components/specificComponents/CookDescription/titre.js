import styled from 'styled-components';
import theme from '../../../utils/theme';

const Apropos = styled.div`
  border-bottom: 2px solid ${theme.colors.cook};
  color: ${theme.colors.cook};
  margin-bottom: 10px;
`;

export default Apropos;
