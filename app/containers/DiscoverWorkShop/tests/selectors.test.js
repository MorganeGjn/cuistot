// import { fromJS } from 'immutable';
// import { makeSelectDiscoverWorkShopDomain } from '../selectors';

// const selector = makeSelectDiscoverWorkShopDomain();

describe('makeSelectDiscoverWorkShopDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
