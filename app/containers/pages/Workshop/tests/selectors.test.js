// import { fromJS } from 'immutable';
// import { makeSelectWorkshopDomain } from '../selectors';

// const selector = makeSelectWorkshopDomain();

describe('makeSelectWorkshopDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
